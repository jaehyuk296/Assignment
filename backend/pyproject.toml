[project]
name = "fastapi-uv-example"
version = "0.1.0"
description = "FastAPI 예제 프로젝트 (uv 패키지 매니저 사용)"
requires-python = ">=3.7"
dependencies = [
    "fastapi",
    "jinja2>=3.1.6",
    "uvicorn",
]
